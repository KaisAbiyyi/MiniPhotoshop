<Window x:Class="MiniPhotoshop.Views.Dialogs.GammaCorrectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gamma Correction"
        Height="260"
        Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}"
        SizeToContent="Height">
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Gamma Correction (s = c * r^gamma)"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}"
                       Margin="0,0,0,16"/>

            <TextBlock Text="Gamma:"
                       FontSize="12"
                       Foreground="{StaticResource TextBrush}"
                       Margin="0,0,0,6"/>
            <TextBox x:Name="GammaBox"
                     Text="0.8"
                     Padding="8"
                     FontSize="14"
                     Margin="0,0,0,12"/>

            <TextBlock Text="Gain (c):"
                       FontSize="12"
                       Foreground="{StaticResource TextBrush}"
                       Margin="0,0,0,6"/>
            <TextBox x:Name="GainBox"
                     Text="1.0"
                     Padding="8"
                     FontSize="14"
                     Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Batal"
                        Click="Cancel_Click"
                        Padding="20,8"
                        Margin="0,0,10,0"
                        MinWidth="80"
                        Background="{StaticResource SurfaceBrush}"
                        Foreground="{StaticResource TextBrush}"/>
                <Button Content="Terapkan"
                        Click="Apply_Click"
                        Padding="20,8"
                        MinWidth="80"
                        Background="{StaticResource AccentBrush}"
                        Foreground="White"
                        BorderBrush="{StaticResource AccentBrush}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
