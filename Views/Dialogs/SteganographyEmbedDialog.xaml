<Window x:Class="MiniPhotoshop.Views.Dialogs.SteganographyEmbedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Steganografi LSB"
        Height="320"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}"
        SizeToContent="Height">
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Sisipkan Pesan (LSB)"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}"
                       Margin="0,0,0,12"/>

            <TextBlock x:Name="CapacityText"
                       Text="Maksimum: 0 bytes"
                       FontSize="11"
                       Foreground="{StaticResource TextBrush}"
                       Opacity="0.7"
                       Margin="0,0,0,12"/>

            <TextBlock Text="Pesan (UTF-8):"
                       FontSize="12"
                       Foreground="{StaticResource TextBrush}"
                       Margin="0,0,0,6"/>

            <TextBox x:Name="MessageTextBox"
                     Height="120"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Batal"
                        Click="Cancel_Click"
                        Padding="20,8"
                        Margin="0,0,10,0"
                        MinWidth="80"
                        Background="{StaticResource SurfaceBrush}"
                        Foreground="{StaticResource TextBrush}"/>
                <Button Content="Terapkan"
                        Click="Apply_Click"
                        Padding="20,8"
                        MinWidth="80"
                        Background="{StaticResource AccentBrush}"
                        Foreground="White"
                        BorderBrush="{StaticResource AccentBrush}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
