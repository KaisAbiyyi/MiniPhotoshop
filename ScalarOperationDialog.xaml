<Window x:Class="MiniPhotoshop.ScalarOperationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Operasi Skalar"
        Height="280"
        Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFF8FAFC"
        SizeToContent="Height">
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Operasi Skalar pada Citra"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>
            
            <TextBlock Text="Pilih Operasi:"
                       FontSize="12"
                       Margin="0,0,0,8"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <RadioButton x:Name="MultiplyRadio"
                             Content="Kali (A × c)"
                             IsChecked="True"
                             Margin="0,0,20,0"
                             VerticalAlignment="Center"/>
                <RadioButton x:Name="DivideRadio"
                             Content="Bagi (A ÷ c)"
                             VerticalAlignment="Center"/>
            </StackPanel>
            
            <TextBlock Text="Nilai Skalar (c):"
                       FontSize="12"
                       Margin="0,0,0,8"/>
            
            <TextBox x:Name="ScalarValueBox"
                     Text="2"
                     Padding="8"
                     FontSize="14"
                     Margin="0,0,0,20"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Batal"
                        Click="Cancel_Click"
                        Padding="20,8"
                        Margin="0,0,10,0"
                        MinWidth="80"/>
                <Button Content="Terapkan"
                        Click="Apply_Click"
                        Padding="20,8"
                        MinWidth="80"
                        Background="#FF4C8BF5"
                        Foreground="White"
                        BorderBrush="#FF4C8BF5"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
